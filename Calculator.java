import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        // TAke in putf from user


        while(true);

        char op = in.next().trim().charAt(0);
        if (op == '+' || op == '-' || op == '*' || op =='/') {
            
        }


    }
}